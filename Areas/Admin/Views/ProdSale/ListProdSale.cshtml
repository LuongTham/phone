@{
    ViewBag.Title = "ListProdSale";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}
@using Phone.Models
@using PagedList.Mvc
@model PagedList.IPagedList<ProdSale>
<link href="~/Assets/admin/css/bootstrap.min.css" rel="stylesheet">
<link href="~/Assets/admin/font-awesome/css/font-awesome.css" rel="stylesheet">
<link href="~/Assets/admin/css/sb-admin.css" rel="stylesheet">
<div class="col-md-12" style="margin-top:10px">
    <div style="margin-bottom:5px;">
        <a href="/Admin/ProdSale/Add" class="btn btn-primary" style="background-color:#d8373e; border: solid 1px #d8373e">Add Product</a>
    </div>
    <div class="panel panel-primary" style="border: solid 1px #d8373e">
        <div class="panel-heading" style="background-color:#d8373e; border: solid 1px #d8373e">List Product</div>
        <div class="panel-body">
            <table class="table table-hover table-bordered">
                <tr>
                    <th style="width:30px;text-align:center">STT</th>
                    <th style="width:100px;text-align:center">Ảnh</th>
                    <th style="text-align:center">Tên sản phẩm</th>
                    <th style="text-align:center">Brand</th>
                    <th style="width:100px;text-align:center">New Price</th>
                    <th style="width:100px;text-align:center">Old Price</th>
                    <th style="width:100px;text-align:center">Thuộc danh mục</th>
                    <th style="text-align:center">Random</th>
                    <th style="text-align:center">Bestseller</th>
                    <th style="text-align:center">Hot Sale</th>
                    <th style="text-align:center">Hot Deals</th>
                    <th style="width:70px;text-align:center">Quản lý</th>
                </tr>
                @{
                    int stt = 0;
                }
                @foreach (var item in Model)
                {
                    stt++;
                <tr>
                    <td style="text-align:center;">@stt</td>
                    <td style="text-align:center;">
                        @* Server.MapPath("folder") -> lay full duong dan vat ly *@
                        @if (System.IO.File.Exists(Server.MapPath("~/Assets/Upload/Product/") + item.img))
                        {
                            <img src="~/Assets/Upload/Product/@item.img" style="width:100px;" />
                        }
                    </td>
                    <td style="text-align:left">@item.name</td>
                    <td style="text-align:left">
                        @{
                            PhoneEntities db1 = new PhoneEntities();
                            var brand = db1.Brands.Where(tbl => tbl.brand_id == item.brand_id).FirstOrDefault();
                        }
                        @if (brand != null)
                        {<text>@brand.Brand_Name</text>}
                    </td>
                    <td style="text-align:left;">@item.newprice</td>
                    <td style="text-align:left;">@item.oldprice</td>
                    <td style="text-align:left;">
                        @{
                            PhoneEntities db = new PhoneEntities();
                            var category = db.Categories.Where(tbl => tbl.id_Category == item.id_Category).FirstOrDefault();
                        }
                        @if (category != null)
                        {<text>@category.name</text>}
                    </td>


                    <td style="text-align:center">
                        @if (item.Random == true)
                        {
                            <span class="glyphicon glyphicon-check"></span>
                        }
                    </td>
                    <td style="text-align:center">
                        @if (item.Bestseller == true)
                        {
                            <span class="glyphicon glyphicon-check"></span>
                        }
                    </td>
                    <td style="text-align:center">
                        @if (item.HotSale == true)
                        {
                            <span class="glyphicon glyphicon-check"></span>
                        }
                    </td>
                    <td style="text-align:center">
                        @if (item.HotDeals == true)
                        {
                            <span class="glyphicon glyphicon-check"></span>
                        }
                    </td>
                    <td style="text-align:center;">
                        <a style="color: #d8373e" href="/Admin/ProdSale/Edit/@item.id_prodSale">Edit</a>&nbsp;
                        <a style="color: #d8373e" href="/Admin/ProdSale/Delete/@item.id_prodSale" onclick="return window.confirm('Are you sure?');">Delete</a>
                    </td>
                </tr>
                }
            </table>
            <style type="text/css">
                .pagination {
                    padding: 0px;
                    margin: 0px;
                    background-color: #d9534f;
                    border: solid 1px #d43f3a;
                }
            </style>
            @Html.PagedListPager(Model, page => Url.Action("ListProdSale", new { page }))
        </div>
    </div>
</div>



